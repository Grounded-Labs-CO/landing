// @ts-nocheck
import { mutation } from "./_generated/server";

export const seed = mutation({
  args: {},
  handler: async (ctx) => {
    // Placeholder para datos semilla de GROUNDED Labs
    return { ok: true };
  },
});
